import Foundation

struct Config {
    // API Keys
    static let geminiAPIKey: String = {
        // Try environment variable first (for development from Xcode)
        if let envKey = ProcessInfo.processInfo.environment["GEMINI_API_KEY"], !envKey.isEmpty {
            return envKey
        }
        
        // For production builds, use the embedded API key
        // Replace "YOUR_GEMINI_API_KEY_HERE" with your actual API key
        let embeddedKey = "YOUR_GEMINI_API_KEY_HERE"
        
        // Check if the embedded key has been replaced
        if embeddedKey == "YOUR_GEMINI_API_KEY_HERE" {
            print("WARNING: API key not configured. AI features will be disabled.")
            return ""
        }
        
        return embeddedKey
    }()
    
    // API URLs
    static let geminiBaseURL = "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent"
    
    // App Configuration
    static let appName = "BoostT1D"
    static let appVersion = "1.0.0"
}
